apiVersion: v1
kind: ConfigMap
metadata:
  name: mitarashi-bot-config
  namespace: ibuki-mitarashi-bot
  labels:
    app: mitarashi-bot
data:
  DEFAULT_TIMEZONE: "Asia/Tokyo"
  # Health server port used by Kubernetes probes
  HEALTH_PORT: "8080"
