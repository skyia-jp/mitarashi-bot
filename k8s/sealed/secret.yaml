# Intentionally left blank.
#
# Use this file only as a temporary scratchpad when creating or rotating
# SealedSecrets. Generate a plain Secret manifest with `kubectl create secret`
# and redirect it into `env.yaml` via `kubeseal`, then wipe this file to avoid
# committing sensitive data.
